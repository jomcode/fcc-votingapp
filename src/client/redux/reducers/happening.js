import * as ActionTypes from '../constants';

const initialState = {};

const happening = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { happening };
