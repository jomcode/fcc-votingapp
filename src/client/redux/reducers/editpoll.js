import * as ActionTypes from '../constants';

const initialState = {
  isFetching: false,
  error: null
};

const editPoll = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { editPoll };
